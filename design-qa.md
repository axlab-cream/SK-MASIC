# 디자인 QA — 등록 리파인 패턴

## 비교 대상

- 원본 시안: `C:\Users\user\Desktop\SK  MAGIC\배너생성기_배너목업_디자인refine_20260910\배너생성기_배너목업_디자인리파인\01_단품형3종\01-기본형.png`
- 원본 시안: `C:\Users\user\Desktop\SK  MAGIC\배너생성기_배너목업_디자인refine_20260910\배너생성기_배너목업_디자인리파인\03_종합배너3종_콘텐츠보강\04-혜택강조-A.png`
- 구현 캡처: `04_output/qa/refine-single-basic-current.png` (1080 × 1350 CSS px, DPR 1)
- 구현 캡처: `04_output/qa/refine-composite-benefit-current.png` (1080 × 1350 CSS px, DPR 1)
- 상태: 단품 1종 / 종합 3종, 상담 CTA·담당자·고지문 표시 상태.

원본은 2160px 고밀도 세로 시안이고 구현은 서비스의 1080 × 1350 출력 규격이다. 같은 논리적 폭 기준으로 내용 영역을 비교했다.

## 확인한 개선 사항

- 단품과 종합 모두 실제 카탈로그의 공식 상품 사진만 사용한다. 코드로 그린 제품 실루엣을 제거했다.
- 종합 7패턴 × 2~12종, 총 77개 실렌더에서 가격 한 줄 줄바꿈·절삭 및 배너 경계 넘침은 0건이다.
- 3종 혜택형은 원본처럼 세로형 상품 카드와 두 개의 혜택 영역으로 분리했다.

## Findings

- [P1] 공식 SK magic 로고 원본 부재
  - Evidence: 프로젝트 `01_spec/assets/logo/`에는 원본 파일이 없으며 구현은 텍스트 대체 표기를 사용한다.
  - Impact: 밝은/어두운 7개 시안 모두 원본과 픽셀 단위 일치가 불가능하다.
  - Fix: 승인된 `logo.svg`와 `logo-white.svg`를 해당 폴더에 제공한다.

- [P2] 아이스 캠페인 배경은 레퍼런스 기반으로 분리한 동적 배경 자산
  - Evidence: `01_spec/assets/backgrounds/ice-campaign-frame.png`에 얼음 프레임과 중앙 여백을 분리해 RSI·RCI의 실제 배경으로 적용했다.
  - Impact: 원본의 제품·문구를 배경에 고정하지 않아, 선택 상품과 가격이 바뀌어도 배너가 유지된다.
  - Fix: 공식 원본 배경이 제공되면 이 자산을 교체해 질감까지 일치시킨다.

- [P2] 원본 시안의 고정 카피와 실제 생성 카피 길이가 다름
  - Evidence: 원본은 특정 캠페인 문구·가격·상품 조합이고 구현은 선택 상품과 검수된 문구를 주입한다.
  - Impact: 같은 구조라도 문단 폭과 줄 수가 달라질 수 있다.
  - Fix: 시안별 카피 길이 상한과 고정 문구 정책을 확정한 뒤 각 패턴의 헤드라인/보조문구 슬롯을 고정값으로 보정한다.

## 필수 충실도 표면

- Typography: 계층·한 줄 가격·상품 이름 처리 확인. 원본 폰트 파일 미수령으로 정확한 글리프 일치는 미검증.
- Spacing/layout: 단품 기본과 종합 혜택의 정보 순서, 카드, CTA, 담당자·고지 영역을 비교했다. 77개 종합 조합에서 경계 넘침 없음.
- Colors: 주황·레드·블루·다크 계열을 패턴별로 분기했다. 원본 팔레트의 공식 값은 미수령.
- Image quality: 공식 상품 사진은 로컬 카탈로그 원본으로 확인. 로고와 얼음 배경은 원본 부재.
- Copy/content: 가격·고지문은 카탈로그/컴플라이언스 코드가 주입하며 LLM이 작성하지 않는다.

## Implementation checklist

1. 승인된 밝은/어두운 로고 원본을 추가한다.
2. 공식 얼음 캠페인 배경 원본을 추가한다.
3. 자산을 넣은 뒤 14개 패턴의 같은 상품·같은 카피·같은 규격 캡처를 원본과 다시 비교한다.

final result: blocked
